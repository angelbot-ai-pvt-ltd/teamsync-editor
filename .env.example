# =============================================================================
# TeamSync Editor - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# =============================================================================
# Domain Configuration
# =============================================================================
# Your main application domain
APP_DOMAIN=app.yourdomain.com

# The subdomain for the office editor
OFFICE_DOMAIN=office.yourdomain.com

# =============================================================================
# Security - JWT Tokens
# =============================================================================
# CRITICAL: Generate a strong random secret (at least 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# Token time-to-live in seconds (default: 1 hour)
TOKEN_TTL=3600

# =============================================================================
# OAuth2/OIDC Configuration
# =============================================================================
# Your OAuth2/OIDC provider issuer URL
# Examples:
#   - Auth0: https://your-tenant.auth0.com
#   - Keycloak: https://keycloak.yourdomain.com/realms/your-realm
#   - Azure AD: https://login.microsoftonline.com/your-tenant-id/v2.0
OIDC_ISSUER=https://auth.yourdomain.com

# OAuth2 Client ID for this application
OIDC_CLIENT_ID=teamsync-editor

# =============================================================================
# S3/MinIO Storage Configuration
# =============================================================================
# MinIO root credentials (admin user)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD

# S3 bucket for document storage
S3_BUCKET=documents

# S3 region (required for AWS SDK compatibility)
S3_REGION=us-east-1

# =============================================================================
# Collabora Online Settings
# =============================================================================
# Spell-check dictionaries (space-separated)
DICTIONARIES=en_US en_GB

# Admin console (DISABLED by default for security)
# Only enable if you need web-based metrics
COOL_ADMIN_USER=
COOL_ADMIN_PASSWORD=

# =============================================================================
# SSL/TLS Configuration
# =============================================================================
# Path to Let's Encrypt certificates
SSL_CERT_PATH=/etc/letsencrypt

# =============================================================================
# Advanced Settings
# =============================================================================
# Log level: trace, debug, information, notice, warning, error, critical, fatal
# LOG_LEVEL=warning

# Maximum concurrent documents
# MAX_DOCUMENTS=100

# Maximum connections per document
# MAX_CONNECTIONS=100
